/** @type {import('next').NextConfig} */

const nextConfig = {
  experimental: {
    appDir: true,
  },
};

module.exports = {
  nextConfig,
  poweredByHeader: false,
  reactStrictMode: true,
  env: {
  },
};
